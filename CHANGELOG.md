# 변경 이력

최신이 위. `vN`은 `index.html`의 캐시 번호 = 배포 번호다 (`node tools/bump.js`가 올린다).

- 왜 그렇게 만들었나 → [PLAN.md](PLAN.md)
- 앞으로 할 것 → [BACKLOG.md](BACKLOG.md)

---

## v113 — 2026-09-04 · ⛰️ 하늘섬

지금 게임엔 **잘한 걸 즉시 갚아주는 가속**이 없었다. 던전은 8칸을 다 맞혀도 8칸이고,
배틀은 빨리 맞혀도 한 대다. 하늘섬은 **연쇄가 곧 높이**다.

- **3연속이면 바람** — 다음부터 두 칸씩 오른다. 끊기지만 않으면 계속 이어진다
- **틀리면 고도 −1** — 죽지 않고 손해만 남는다. 되돌리려면 다시 올라와야 한다
- **6칸마다 🏝️ 섬** — 그 아래로는 안 떨어진다. 섬에서 또 틀리면 섬이 무너져 추락.
  땅(고도 0)은 안 무너져서 **첫 구간은 규칙을 배우는 연습장**이 된다
- **정상 24칸** — 무한 기록은 투기장이 이미 갖고 있다. 아이에겐 "이겼다"가 필요하다
- **시간은 발밑 구름** — 게이지가 아니라 서 있을수록 옅어지는 구름이다.
  던전(6→3.4초)보다 훨씬 느슨한 9→5.5초 — 여기서 조이는 건 시간이 아니라 고도다
- **단어는 ★이 높은 것부터** — 던전(오답노트)과 정반대. 아는 걸 자랑하는 무대
- **골드는 새로 오른 칸에만** — 안 그러면 오르내리며 파밍이 된다
- 마을 **북쪽 길 끝**에 하늘섬 관문. 마을에서 제일 먼 곳이 제일 나중에 열린다
- 새 파일 `js/sky.js`, `BAL.sky`, `#screen-sky`. `ZONES`의 `sky`를 `ready: true`로

테스트 202 → 218개.

---

## v32 — 2026-09-01 · 투기장에 긴장감과 나갈 길

"그만두고 싶은데 죽으려니 한참 걸린다" — 나가려면 죽어야 하는데
처음엔 20번을 틀려야 죽었다. 재미가 아니라 갇힌 상태였다.

- **🏳️ 여기까지 버튼** — 배틀 화면 왼쪽 위. 지금까지의 골드·경험치는 그대로고
  (잡을 때마다 이미 받는다) 기록도 남는다. 최고 기록이면 폭죽까지 터진다.
  일반 층·보스 배틀에는 안 뜬다 (`onRetire`를 넘긴 판에만)
- **제한시간이 조여든다** — 8초에서 시작해 마리당 0.15초씩 줄어 5초에서 멈춘다
  (일반 배틀은 10초 고정). 제한시간을 판마다 받도록 `Battle.start`를 고쳤다
- **몬스터가 세진다** — `atk` 0.05 + 0.012/마리 → **0.08 + 0.018/마리**.
  Lv12 기준 1마리째 13번(전엔 20번) → 11마리째 4번 → 21마리째 3번
- 투기장 몬스터 공격력 기준을 `playerMaxHp()` → `hpAt(lv)`로.
  타워는 이미 그렇게 고쳤는데 투기장만 남아서, HP를 올려주는 보상이 여기서만 상쇄됐다

테스트 105 → 119개.

---

## v31 — 2026-09-01 · 성장 곡선 정리

기능을 하나씩 붙이는 동안 수치를 따로따로 정해서, 합쳐 놓으니 세 군데가 어긋나 있었다.
전부 `js/balance.js` 안에서 고쳤다.

**① 필살기가 게임을 지우고 있었다**
- 카드를 다 모으면(465pt) **20.6배** → 모든 타워의 최종 보스가 한 방에 즉사했다.
  연속 정답 3번 + 스펠링 하나면 보스전이 끝난다. 공부를 열심히 할수록 게임이 사라지는 구조
- `ultPerCardPt` 1/25 → **1/120**, `ultMax: 6` 상한 추가
- 이제 0장 2.0배 · 절반 3.9배 · 전부 **5.9배**. 일반 몬스터는 한 방(통쾌함 유지),
  보스는 2~3방. 타워가 계속 늘어도 상한에서 멈춘다

**② 레벨이 콘텐츠를 못 따라갔다**
- 55층을 전부 한 번씩 깨도 **Lv15**인데 곰 탑 3은 Lv25~48을 상정 → 로비에 계속 ⚠️
- `40 × Lv^1.4 + 20` → **`22 × Lv^1.15 + 15`**
- 이제 전부 깨면 **Lv24**. 타워가 11개까지 늘면 Lv52에 닿는다

**③ 몬스터가 너무 약했다**
- 바벨 1층에서 **17번**을 틀려야 죽어 긴장감이 없었다
- `atk.base` 0.06 → **0.09**, `perFloor` 0.004 → 0.005
- 실수 벌칙도 함께: 미로 오답 5% → 7%, 러너 충돌 6% → 8%, 금고 오답 5% → 7%
- 이제 **5~10번**

**④ 곰 탑 tier 결정 — "tier는 반(班)에 매긴다"**
- 곰 탑 3(2.5)이 호랑이(2.3)보다 어려워 동물 서열이 깨져 있었다
- 같은 동물이면 🔥 개수가 같아야 한다. 권수는 난이도가 아니라 단어 양의 차이다
- 곰 탑 1/2/3 = **1.5 / 1.55 / 1.6** (전부 🔥🔥, 독수리 1.7보다 낮게)
- lvRange도 [5,24] / [6,25] / [7,26]으로. 곰 탑 2·3이 "권장 레벨 도달 불가"였던 것도 풀렸다
- 오라의 난이도 조건도 새 눈금에 맞춤 (2.0 → 1.5 / 1.7 / 2.1 = 곰 / 독수리 / 사자).
  지금 콘텐츠로 **8종** 도달 가능, 2종은 다음 타워의 목표

**테스트 96 → 98개** — 「성장 곡선 — 게임 전체가 말이 되는가」 섹션을 새로 넣었다.
개별 수치가 아니라 합쳤을 때 놀 만한지를 본다: 필살기가 보스를 한 방에 못 지우는지,
콘텐츠를 다 깨면 권장 레벨에 닿는지, 몇 번 틀리면 죽는지가 3~12번 안인지.

---

## v30 — 2026-09-01 · 발음 읽어주기를 난이도에서 떼어냄

정찰은 처음 만나는 단어를 보여주는 자리인데 발음이 안 나왔다.
원인은 발음이 "듣기 문제"(기본 OFF) 설정에 묶여 있어서 —
**발음을 들으려면 배틀 난이도까지 올려야 하는 구조**였다.
그대로 두면 아이가 발음을 한 번도 못 듣고 눈으로만 외우게 된다.

- 설정을 둘로 분리
  · 🗣️ **발음 읽어주기** (`say`, 기본 **ON**) — 학습 도움. 난이도와 무관
  · 🎧 **듣기 문제** (`listen`, 기본 OFF) — 배틀 난이도 옵션
  · 옛 저장에도 발음이 켜진 채로 채워진다 (`fillShape`)
- 정찰: 카드가 뜨면 자동으로 읽어주고, **🔊 버튼은 설정과 무관하게 항상** 있다
- 정찰 요약: 단어 칩을 누르면 그 단어를 다시 들려준다 (헷갈리는 것만 골라 듣게)
- 도감: 가진 카드를 누르면 발음이 나온다
- 배틀: 영어가 이미 보이는 문제(w2m)의 🔊는 항상 제공.
  답이 새는 뜻→영어(m2w)에는 넣지 않았다
- `englishVoice()` — 한국어 기기에서 한국어 목소리가 영어를 읽어버리는 걸 막는다.
  크롬은 목소리 목록이 늦게 채워져서 `onvoiceschanged`로 다시 읽는다
- 읽기 속도를 `BAL.speech.rate`(0.85)로 빼서 조절 가능하게

테스트 86 → 96개. 옛 저장 마이그레이션과 TTS 없는 기기에서의 동작을 확인한다.

---

## v29 — 2026-09-01 · 🐻 곰 탑 3

교재 단어 묶음 Vocabulary 1~4. 4단원 47단어 10층(보스 2),
tier 2.5, lvRange [25, 48], 정복 보너스 최대HP +25% · 칭호 "숲의 지배자".

**책과 다르게 넣은 것 두 가지** (둘 다 파일 주석에 적어 뒀다)
- `crash` — 책에 Voca 1(명사·굉음)과 Voca 2(동사·충돌하다)로 **두 번** 나온다.
  게임은 영어 철자를 카드 키로 쓰기 때문에 그대로 넣으면 카드가 하나로 합쳐지고
  ★도 공유된다. → Unit 1에 두 뜻을 합쳐 한 번만 (그래서 Unit 2는 11단어)
- `pass out` — 책 뜻이 "의식을 잃다, 기절하다"인데 같은 단원 `faint`(기절하다)와
  겹쳐 4지선다에 정답이 둘 생긴다. → "의식을 잃다"로 좁혔다

**같이 달라진 것**
- 전체 카드 192장 378pt → **239장 465pt**. 오라 도달 가능 7종 → **8종**
  (🌈 무지개 210장이 열렸다. 🌙 달빛 250 · 🐉 용 300은 다음 타워의 목표로 남는다)
- 최고 티어 2.0 → 2.5

**남긴 문제** → [BACKLOG](BACKLOG.md)
- 곰 탑 3(2.5)이 호랑이(2.3)보다 어려워서 **동물 서열이 깨진다.**
  표는 tier를 반에 매기는데(곰=1.5) 데이터는 권수마다 올린다(1.5→2.0→2.5)
- 필살기 배율이 17.1 → **20.6배**로 또 올랐다

---

## v25 — 2026-09-01 · 층 스테이지 조합 + 🔐 금고 방

미니게임을 늘리기 전에, **개수가 아니라 조합이 다양성을 만든다**는 쪽으로 구조를 먼저 바꿨다.
45층을 매번 `미로 → 러너` 같은 순서로 가면 아무리 좋은 게임도 지겹다.

**스테이지 파이프라인**
- 층 = [탐험 파트] + [마무리 파트]. 층마다 `BAL.stages` 풀에서 하나씩 뽑는다.
  같은 탐험 파트가 두 층 연달아 나오지 않는다
- `maze.js`가 `Runner.start()`를 직접 부르던 것을 걷어냈다. 화면이 화면을 부르면
  조합을 바꿀 수 없다. 이제 스테이지는 `(run, done)` 하나만 지키고, 순서는 `Game.nextStage()`가 정한다
- 미니게임 추가 = 화면 파일 + `Game.STAGES` 등록 + `BAL.stages`에 id 한 줄

**🔐 금고 방** (`js/vault.js`) — 탐험 파트의 두 번째 선택지
- 자물쇠(뜻) 4~6개 + 단어 카드(영어), 오답 카드 2장이 섞여 있다
- 카드 탭 → 자물쇠 탭. 맞으면 열리고, **틀리면 그 카드가 부서져 영영 못 쓴다**
- 중간쯤 열면 금고지기가 깨어나 배틀 1회 (탐험 파트가 배틀을 품는 구조 유지)
- 못 열어도 진행은 안 막는다. 답을 보여주고 넘어간다
- 미로가 "하나를 고르는" 것이라면 금고는 "여럿을 동시에 놓고 배치하는" 것 —
  확실한 것부터 꽂고 애매한 건 소거법으로 좁히는, 지금 없던 능력을 시험한다

테스트 73 → 84개. 조합이 모두 나오는지, 연속 중복이 없는지,
파이프라인이 뽑은 스테이지를 전부 지나 층 클리어까지 가는지 확인한다.

---

## v24 — 2026-09-01 · 타워 난이도를 화면에 표시

오라 조건에 "티어 1.5 이상 타워"라고 써 놓고, 정작 **어느 타워가 몇 티어인지
볼 수 있는 곳이 없었다.** "티어"라는 말도 아이한테는 개발자 용어다.

- `tierFire()` — 티어를 🔥 개수로 (1.0 → 🔥 · 1.5 → 🔥🔥 · 2.0 → 🔥🔥🔥).
  ⭐은 단어 마스터리에 이미 쓰고 있어서 🔥을 골랐다
- 로비 타워 카드에 `난이도 🔥🔥 · 권장 Lv.5~24`
- 오라 조건도 전부 같은 표시로 (`난이도 🔥🔥 이상 타워 정복`)
- 오라 미리보기에서 **어느 타워를 깨면 되는지 이름으로** 알려준다
  ("📘 곰 탑 2 타워 또는 📗 곰 탑 1 타워를 꼭대기까지 깨면 돼요")
- 용어 정리: **정복** = 꼭대기까지 깸 / **완전 정복** = 카드를 전부 모음
- 테스트 추가 — 난이도 표시가 단조 증가하는지, 오라의 난이도 조건을
  채워 줄 타워가 실제로 존재하는지 (없으면 영영 못 여는 오라가 된다)

---

## v23 — 2026-09-01 · 꾸미기·로비에서도 오라 목표 보기

오라를 마일스톤으로 바꾸면서 도감에만 목록을 넣었는데, 정작 오라를 고르러
가는 곳은 꾸미기(🎨)다. 거기선 가진 것만 보이고 안내문도
"단원을 완성하면 열려요"라는 옛날 설명이 남아 있었다.

- 꾸미기: 못 가진 오라도 🔒과 조건으로 표시. 누르면 내 캐릭터가 입은 모습 미리보기
- 꾸미기·로비에 "다음 ✨반짝이 오라까지 카드 15장" 한 줄

---

## v22 — 2026-09-01 · 곰 탑 2 타워 + 오라 재설계

**추가**
- **📘 곰 탑 2 타워** (`data/dsd2.js`) — 교재 단어 묶음 Vocabulary 1~8.
  8단원 96단어 20층(보스 4), tier 2.0, lvRange [15, 36], 정복 보너스 공격력 +20%.
  책에는 Vocabulary 1~4가 두 벌 있어서 뒤 챕터를 unit 5~8로 이어 붙였다.
  전설 카드 4장(`embarrassed` `be supposed to` `power failure` `suspiciously`).
  전체 카드가 96장 172pt → **192장 378pt**가 됐다.

**바뀜 — 오라를 타워에서 떼어냈다**
- 단원 1개 = 오라 1개였는데, 곰 탑 2를 넣으니 단원 18개 vs 오라 10종이 됐다.
  재사용하면 "🎉 새 오라!"라며 이미 가진 걸 주게 되고, 새로 그리자니 타워를 추가할 때마다
  8종씩 필요하다. 타워를 늘릴수록 심해지는 구조라 근본을 바꿨다.
- 이제 오라는 **전체 카드 수 + 티어 조건** 마일스톤으로 열린다 (`AURAS[].need`).
  어느 타워에서 모았든 합쳐서 세고, 상위 오라는 티어 1.5 / 2.0 이상 타워를 끝까지 깨야 한다.
  → 지금 콘텐츠로 7종, 나머지 3종은 다음 타워의 목표. **타워 데이터에 오라 필드가 사라졌다.**
- 단원 완성 보상은 **골드**로 (`BAL.gold.unitComplete` × 티어).
- 도감 위쪽에 오라 10종의 조건과 진행도를 보여주는 띠를 추가.

**도구**
- `tools/test.js`가 `data/` 파일을 하드코딩하고 있어서 새 타워가 검사에서 빠졌다.
  폴더를 통째로 읽도록 수정 — 이 덕에 오라 충돌을 잡았다. 테스트 51 → 66개.
- `tools/validate.js`가 오라 마일스톤이 지금 콘텐츠로 닿는지 확인한다.
  전부 닿으면 "다음 타워 목표가 없다"고, 하나도 못 닿으면 오류로 알려준다.

**알아낸 것 (→ BACKLOG)**
- 필살기 위력이 전체 카드 포인트에 비례해서 172 → 378pt로 **8.9배 → 17.1배**가 됐다.
  타워를 추가할 때마다 무한정 세지는 구조다.
- 곰 탑 2 20층 보스가 Lv15 은검으로 31방, Lv36 용의검으로도 16방. 보스전이 너무 길다.

---

## v20 — 2026-09-01 · 프로젝트 체계화

게임 내용은 그대로. 앞으로 커질 것에 대비해 관리 장치를 넣었다.

**추가**
- `js/balance.js` — 난이도·보상·속도 수치를 한 파일로 모음. 5개 파일에 흩어져 있던
  `TIME_LIMIT`·`BOSS_GATE`·`MIN_LOOK`·러너 속도·미로 크기·골드/경험치 공식이 전부 여기로.
  이제 밸런스를 바꿀 때 이 파일만 연다.
- `tools/bump.js` — `index.html`의 캐시 번호 17곳을 한 번에 올리고 검사까지. 커밋 전 한 줄.
- `tools/validate.js` — 타워 데이터 검사. id 중복·단어 중복·단원 번호 중복·단어 8개 미만·
  뜻이 겹쳐 오답 보기가 안 되는 단어·`auras` 이름과 개수·`tier`/`lvRange`/`clearBonus` 형식,
  그리고 `index.html`에 빠진 `<script>` 태그와 섞인 캐시 번호.
- `tools/test.js` — 브라우저 없이 도는 회귀 테스트 51개. PLAN의 밸런스 검증표를 코드로 옮겨서,
  수식을 손대면 어긋난 자리를 바로 알려준다.
- **세이브 백업** — 불러오기·초기화·저장 손상 직전의 상태를 하나 보관.
  로비 → 💾 저장코드 → 🩹 백업에서 되돌릴 수 있다.
- **세이브 버전 체계** — `SAVE_VERSION` + `MIGRATIONS`. 저장 구조에서 필드의 뜻이 바뀔 때
  순서대로 변환한다. 더 새 버전에서 만든 세이브는 건드리지 않는다.
- 문서를 셋으로 분리: PLAN(설계) · CHANGELOG(이력) · BACKLOG(할 일).

**고침**
- 필살기의 스펠링 타일 UI가 각인 시험과 복붙돼 있던 것을 `Cards.spellTest` 재사용으로 통일
  (battle.js 229 → 199줄). 시험 UI를 고치면 두 곳에 함께 반영된다.
- PLAN.md의 낡은 수치를 코드와 동기화 — 상점표(물약 60G/40% 회복, 무기는 곱하기 %),
  적 피해 공식(`내최대HP` → `기본HP(내Lv)`), Lv20 스킬(보스 필살기 → 필살 강화 1.5배).
  섹션 번호 중복(11이 둘)과 "완료" 섹션에 섞여 있던 미착수 항목도 정리.

**알아낸 것**
- 곰 탑 1의 `reply`와 `answer`가 둘 다 뜻이 "대답하다"라 서로 오답 보기가 되지 못한다
  (validate.js가 잡음). → BACKLOG
- 설계 레벨 구간 안에서 레벨업의 체감이 예상보다 작다 (Lv5→24에 필요 타격이 3.86 → 3.05방,
  정수로는 계속 4방). → BACKLOG

---

## 2026-09-01

- **미로 문이 길을 막던 문제 수정** — 문을 막다른 길에만 배치. 막다른 길이 없으면
  문이 통행을 막지 않는다 (열쇠 주우러 되돌아가는 일이 없게).
- **🔍 정찰(예습) + ⚡ 이건 알아! 지름길** — 층에 들어가기 전 그 층 단어를 한 장씩 보여주고
  마지막에 요약. "다음"은 0.8초 뒤에 켜진다(마구 넘기기 방지). 카드가 있는 단어·보스 층·
  이미 깬 층 재도전은 생략, 설정에서 끌 수 있다.
  정찰 중 아는 단어는 그 자리에서 스펠링을 맞혀 ★★★ + 카드를 즉시 딸 수 있다 —
  단 정규 각인 시험보다 어렵게(도움 글자 없음, 함정 글자 2~5개, 층당 1회).
- **4단계(2) 전투 연출 + 필살기** — 캐릭터가 전장에 서 있다가 정답이면 달려가 벤다
  (크리티컬은 금빛 잔상), 오답이면 몬스터가 박치기.
  필살기 게이지: 연속 정답 3번 → 스펠링 특별 문제 → 맞히면 대미지 폭발, 틀려도 피해 없음.
  위력 = 공격력 × (2 + 카드포인트/25) → 카드 0장 2배, 172pt 전부면 8.9배.
  Lv20 스킬을 보스 전용 필살기에서 **필살 강화(위력 1.5배)**로 역할 변경.
- **도감에서 잠긴 오라 미리보기** — 단원 헤더의 배지를 누르면 내 캐릭터가 그 오라를 입은 모습을 보여준다.
- **3b 세트 보상** — 오라 코스튬 10종 + 타워 정복 칭호/보너스.
  단원 완성 → 오라 1종 해금. 타워 전체 정복 → 칭호 + 영구 %(바벨 공격력 +10%, 곰 탑 1 최대HP +15%).

## 2026-08-31

- **3a 단어 카드** — ★★★ 달성 → 스펠링 각인 시험 → 카드 획득. 도감, 보스 관문(단원 카드 60%),
  복습 배틀(허수아비 ATK 0). 등급은 영어 글자 수 + 구 여부로 자동 판정해서 새 타워도 그대로 작동.
  기존 저장의 ★★★ 단어는 접속 시 대기열로 소급(`backfillCards`).
- **밸런스 개편** — 몬스터를 절대 레벨이 아니라 **타워의 기준 레벨**로 만들고,
  무기를 더하기에서 **곱하기(%)**로, 골드 경제를 재조정.
  바벨에서 키운 캐릭이 새 타워 1층에서 한 방에 죽던 문제를 구조적으로 해결.
  적 피해를 `playerMaxHp()`(보너스 포함)에서 `hpAt(lv)`(기본 HP) 기준으로 바꿔,
  HP를 올려주는 보상이 스스로 상쇄되던 것도 함께 고침.
- **4단계(1) 축하 연출** — 층 클리어·레벨업 폭죽(`UI.confetti`), 팡파레, 결과창 등장 애니메이션.
  보스/타워 정복일수록 화려하게. 결과창 단어 목록은 ★ 낮은 순 12개까지만.
- **3단계 로비/배경** — 타워 카드 탑 일러스트(깬 층만큼 창문에 불이 켜지고 정복하면 금색 깃발),
  밤하늘 별 48개 + 도시 실루엣, 러너 배경 패럴랙스.
  미로 이동을 격자와 분리된 스프라이트 레이어로 재구성 → 미끄러지듯 걷고, 진행 방향으로 몸이 돌고,
  펫이 한 박자 늦게 따라온다.
- **2단계 몬스터 & 배틀 연출** — 몬스터 9종 + 보스 5종 + 펫 3종 SVG.
  타격 시 흰 번쩍 + 움찔 + 베기 이펙트, 크리티컬 시 화면 번쩍, 처치 시 회전·축소 소멸.
  미로에도 SVG 몬스터, 캐릭터는 무기를 든 전신, 펫이 한 칸 뒤를 따라다님.
- **1단계 SVG 레이어 캐릭터** — 이모지 → 레이어 조합 SVG(머리 4종 × 머리색 6 × 피부 3).
  첫 실행 시 캐릭터 만들기, 코스튬·모자·무기가 캐릭터에 실제로 그려짐.
- 러너: 미션 단어를 캔버스 안 상단 띠로 이동, 부딪힘 안내도 캔버스 안으로(단어 가림 방지), 속도 완화.
- 배틀 제한시간이 실제로 작동하도록 수정.
- 캐시로 새 파일이 안 실려 캐릭터 기능이 동작하지 않던 문제 수정.
- 동의어(같은 뜻 단어)가 오답 보기로 나오는 버그 수정.
- 책 타워를 교재 단어 묶음으로 (Vocabulary 1~4, 48단어). 샘플 책 타워 제거.
- **최초 프로토타입** — 미로 → 배틀 → 러너의 한 층 흐름, 로비, 상점, localStorage 저장.
