# 할 일

설계 의도는 [PLAN.md](PLAN.md), 지나간 것은 [CHANGELOG.md](CHANGELOG.md).

우선순위는 위에서 아래로. **끝나면 지우지 말고 CHANGELOG로 옮긴다.**

---

## 지금 — 아이 테스트가 먼저

게임을 더 만들기 전에, 만든 게 재미있는지부터 확인한다.

- [ ] 아이 실제 교재 단어 20~30개로 책 타워 채우기 → 태블릿으로 플레이 → 관찰
- [ ] 관찰 기록을 `docs/playtest/YYYY-MM-DD.md`에 짧게 (몇 층에서 지루해했나, 어디서 소리 질렀나,
      어디서 막혔나). 이 게임의 유일한 테스터가 남기는 데이터라 제일 값지다
- [ ] 관찰 결과로 밸런스 조정 — `js/balance.js`만 고치면 된다

## 콘텐츠에서 발견된 것

- [ ] DSD1의 `reply`와 `answer`가 뜻이 둘 다 "대답하다" → 서로 오답 보기가 되지 못한다.
      한쪽을 "답장하다" 같은 식으로 구분 (`node tools/validate.js`가 알려줌)
- [ ] 메인 타워 이름 확정 (후보: Babel, Wordspire, Lexicon, 마법서고)

## 밸런스에서 살펴볼 것

- [ ] **설계 구간 안에서 레벨업 체감이 작다.** DSD1(구간 Lv5~24) 5층 몬스터를 잡는 데
      Lv5에 3.86방, Lv24에 3.05방 — 정수로는 20레벨 내내 4방이다.
      원인: 공격력 `10 + Lv×3`의 상수 10 때문에 실제 성장비가 `growth: 0.7`보다 더 눌린다.
      선택지 — ① `growth`를 0.55쯤으로 낮춘다 ② `atkBase`를 줄이고 `atkPerLv`를 올린다
      ③ 지금이 의도라면 PLAN의 "레벨업이 체감됨" 문구를 현실에 맞게 고친다.
      어느 쪽이든 `tools/test.js`의 검증표가 같이 바뀌어야 한다

## 콘텐츠 확장

- [ ] **DSD2 타워** — tier 2.0, lvRange [15, 36]. `data/` 파일만 만들면 됨 (설계 완료)
- [ ] **지하 던전 (Lv10 + 카드 25장)** — 로그라이크 생존. HP 회복 없이 계속 내려가기,
      3층마다 축복 선택, 언제든 탈출해 보상 확정. 지금 `ZONES`에서 `ready: false`
- [ ] **하늘섬 (Lv20 + 카드 60장)** — 점프 액션. 맞는 단어 발판만 밟으며 위로,
      발판은 계속 흘러내림. 지금 `ZONES`에서 `ready: false`
- [ ] 예문(`ex`) 있는 단어용 "빈칸 채우기" 모드
- [ ] 한 기기 2인 대전 모드

## 연출

- [ ] 펫 애니메이션 강화

## 2단계 (게임이 재밌다고 검증된 후에만)

- [ ] 닉네임 + PIN 로그인 (초등생에게 이메일 가입은 안 시킨다), 클라우드 저장
- [ ] 랭킹, 레이드 보스 (반 전체가 같이 HP 깎기)
- [ ] 학원 선생님용 대시보드 (누가 몇 층, 어떤 단어를 많이 틀리나)

> 실시간 동시 플레이는 **안 한다.** 과하고 불안정하다. 비실시간만으로 "같이 하는 느낌"은 충분하다.

## 관리 체계 (여유 있을 때)

- [ ] `cards.js`·`preview.js`가 `Lobby.render()`를 직접 부르는 레이어 규칙 위반을
      콜백으로 정리 (PLAN 11장)
- [ ] 파일이 25개를 넘어가면 ES module 전환 검토.
      단 `index.html` 더블클릭 실행이 막히므로 그 비용을 감수할 만할 때만
