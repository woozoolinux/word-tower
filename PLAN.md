# Word Tower — 기획 문서

> 초등학생용 영어 단어 학습 게임. 책(교재)의 단어를 기반으로, 탑을 한 층씩 정복하며 단어를 익힌다.
> 작성 2026-08-31 · 최신 반영 2026-09-01

**이 문서는 "왜 이렇게 만들었나"만 담는다.**

| | |
|---|---|
| 지나간 변경 | [CHANGELOG.md](CHANGELOG.md) |
| 앞으로 할 것 | [BACKLOG.md](BACKLOG.md) |
| 난이도·보상 **수치** | [`js/balance.js`](js/balance.js) — 이 문서는 원리만 쓴다 |
| 타워 추가 방법 | [README.md](README.md) |

---

## 1. 목표

- **재미가 먼저.** 퀴즈에 껍데기를 씌운 게임이 아니라, *게임 액션 자체가 단어 판단*이 되게 한다.
- 아이가 **계속 켜고 싶게** 만든다: 눈에 보이는 진행(층수), 짧은 세션(한 층 2~3분), 수집/성장(레벨·장비·펫).
- 웹 브라우저에서 바로 실행. 설치 없음. 링크 하나로 폰·태블릿·PC 모두.
- 단어 데이터는 **파일로 분리** — 책에서 뽑은 단어를 넣기만 하면 새 타워가 생긴다.

## 2. 저작권 정리

- 단어 자체(apple, brave…)와 단어 목록은 저작권 보호 대상이 아님.
- **하지 말 것:** 책의 예문·삽화·지문·문제 복사, 책 제목/출판사 로고를 게임 브랜드처럼 사용,
  책의 단원 구성 + 배열 + 뜻풀이 통째 복제.
- **안전한 구조:** 단어 + 직접 쓴 뜻/예문 + 우리 방식의 층 구성.
  단어 데이터는 사용자가 넣는 방식(= 학원도 자기 교재로 사용 가능).
- 공개 배포 시 책 타워 이름은 "○○ 단어팩" 정도로 중립 표기.

## 3. 타워 구조

```
🏰 로비
 ├─ ⭐ 바벨 타워            ← 기초 단어. 누구나 도전
 ├─ 🐻 곰 탑 1             ← 교재 진도용
 ├─ 🐻 곰 탑 2
 └─ (타워 추가 = 단어 파일 추가)
```

### 이름 규칙 — 동물 월드

> **등급은 동물, 권수는 숫자.** 예: DS-D 2권 → `🐻 곰 탑 2`

학원 반 등급(IS ~ MS-B)을 동물 서열에 대응시킨다.
책 제목·학원 이름을 쓰지 않아 저작권·상표 문제가 없고,
**아이가 설명 없이도 순서를 안다.**

| # | 반 | 이름 | 세계 | tier | lvRange |
|---|---|---|---|---|---|
| 1 | IS | 🐣 병아리 탑 | 🌱 새싹 들판 | 1.0 | [1, 10] |
| 2 | DS-A | 🐰 토끼 탑 | 🌳 숲 | 1.15 | [2, 13] |
| 3 | DS-B | 🦊 여우 탑 | 🌳 숲 | 1.3 | [3, 17] |
| 4 | DS-C | 🐺 늑대 탑 | 🌳 숲 | 1.4 | [4, 20] |
| 5 | **DS-D** | **🐻 곰 탑** | 🌳 **숲의 왕** | 1.5 | [5, 24] |
| 6 | LS-A | 🦅 독수리 탑 | 🏔️ 야생 | 1.7 | [8, 28] |
| 7 | LS-B | 🐆 표범 탑 | 🏔️ 야생 | 1.9 | [11, 32] |
| 8 | LS-C | 🦁 사자 탑 | 🏔️ 야생 | 2.1 | [14, 36] |
| 9 | LS-D | 🐅 호랑이 탑 | 🏔️ 야생의 왕 | 2.3 | [17, 40] |
| 10 | MS-A | 🐉 드래곤 탑 | 🌋 전설 | 2.6 | [21, 46] |
| 11 | MS-B | 🔥 불사조 탑 | 🌋 전설의 정점 | 3.0 | [25, 52] |

### 권(卷) 배치 공식 — 한 등급에 6권

교재는 한 등급마다 6권이다. 그래서 등급마다 **0.10의 자리**를 주고 권은 **0.02씩** 채운다.

```
tier     = 등급base + 0.02 × (권 − 1)          → 6권이 딱 들어가고 다음 등급과 안 겹침
lvRange  = [ lo + ⌊(권−1)/2⌋ , hi + (권−1) ]   → 권이 오를수록 권장 레벨이 조금씩 위로
```

예 — DS-C(늑대) 6권: tier 1.36 · 1.38 · 1.40 · 1.42 · 1.44 · 1.46,
lvRange [4,20] → [4,21] → [5,22] → [5,23] → [6,24] → [6,25].
다음 등급 DS-D(곰)는 1.50에서 다시 시작한다.

*현재 만들어진 것: 4번 늑대 탑 1~6, 5번 곰 탑 1~3*

**설계 의도 세 가지**

1. **각 세계의 마지막이 그 세계의 왕** — 곰(숲의 왕), 호랑이(야생의 왕).
   세계가 바뀌는 지점이 눈에 띄어 "한 구간을 끝냈다"는 감각이 생긴다.
2. **전설 구간에서 실제 동물 → 상상 속 동물로 도약** — 호랑이 다음이 드래곤.
   "여기부터는 차원이 다르다"를 이름만으로 전달한다.
3. **아이가 설명 없이 순서를 안다** — 병아리 < 곰 < 호랑이는 초등생도 다 안다.
   난이도 표시(🔥 개수)나 권장 레벨을 읽지 않아도 서열이 읽힌다.

### 🏰 로비 — 탑이 66개가 되어도 쓸 수 있게

등급 11개 × 6권이면 탑이 66개다. 평평하게 늘어놓으면 스크롤 지옥이라 **등급 단위로 접는다.**
"숨기는" 게 아니라 "접는" 것이다 — 한 등급의 6권은 서로 막지 않기로 했고(학원이 3권을 내주면
1·2권 없이 바로 해야 한다), 잠긴 탑도 문은 두드릴 수 있어야 한다.
접힌 건 한 번 눌러 펴면 되지만, **숨긴 건 존재를 모른다.**

세 층으로 정리한다:

1. **▶ 이어서 하기** — 마지막에 들어간 탑과 층으로 한 번에. 실제 플레이의 대부분이 "하던 거 계속"이다
   (`towerProg(id).at` · `touchTower` / `lastTower`)
2. **등급 카드** — 접혀 있어도 규모(권수·단어 수·잠긴 수)와 왕 상태는 보인다. 목표가 보여야 모은다.
   자동으로 펴지는 등급: 진행 중인 탑이 있는 곳 / 강행한 탑이 있는 곳 / 마지막에 하던 곳.
   아무 데도 안 갔으면 **들어갈 수 있는 가장 낮은 등급** 하나는 펴 둔다(빈 화면을 주면 안 된다)
3. **등급 안** — 하던 권을 위로, 아직 안 간 권은 `+ n권 더 보기`, 정복한 권은 `🏆 정복 n권`으로 접기

접힘 상태는 `state.ui`에 둔다. 설정이 아니라 "지금 어디를 보고 있나"라서 `settings`와 섞지 않는다.

**왕의 방은 없애지 않고 메달 한 줄로 줄였다.** 왕은 이 게임의 보상 구조 전체(다음 등급·칭호·
해금 연출)라 등급 카드 안에 접어 두면 아이가 존재를 모른다. 트로피 벽은 남기고 부피만 줄인다.
등급 카드 헤더에도 왕 상태 한 줄이 있어 진입점은 두 곳이다.

### ✨ 오라는 두 벌로 그린다

로비·꾸미기 화면은 **인라인 SVG**라 CSS 애니메이션이 돈다. 그런데 마을·던전에서는
캐릭터를 `Avatar.image()`로 **래스터 이미지**로 그린다 — 그 안의 CSS는 죽는다.
그래서 오라가 장착돼 있어도 마을에서는 안 보였다.

캔버스에는 `Aura.paint(ctx, x, y, id, t, layer)`로 **직접 그린다**. 렌더 루프가 있으니
오히려 더 살아 있는 효과를 낼 수 있다 — 발밑에서 피어오르는 불, 퍼지는 물결, 퍼덕이는
날개, 도는 별, 번쩍이는 번개. `layer`는 캐릭터 뒤(`back`)와 앞(`front`)을 나눈다.

두 벌을 유지하는 대가는 있지만, 하나로 합치려면 마을에서 캐릭터를 인라인 SVG로 띄워야
하고 그러면 캔버스의 깊이 정렬·카메라가 무너진다.

### 💥 타격감 (`js/hitfx.js`)

때리는 맛은 그림이 아니라 **반응**에서 나온다. 좋은 에셋을 써도 맞는 순간 아무 일도
안 일어나면 허공을 치는 느낌이다. 그래서 에셋과 무관하게 코드로 만든다.

| | |
|---|---|
| **히트스톱** | 맞는 순간 CSS 애니메이션을 잠깐 멈춰 그 자세를 붙잡는다 — **가장 큰 요소** |
| **넉백** | 맞은 쪽이 밀렸다 튕겨 돌아온다 (`--kx`/`--ky`) |
| **화면 펀치** | 무대가 순간 커졌다 작아진다. 크리티컬은 살짝 기울기까지 |
| **파편** | 부딪힌 자리에서 조각이 사방으로 튄다 |
| **붉은 화면** | 내가 맞으면 화면 가장자리가 붉게 번진다 |

전부 **0.05~0.2초**여야 한다. 길면 답답해진다. 세기는 `power`로 조절한다 —
평타 1, 크리티컬 1.6, **필살기 2.4**. 카드를 모은 보람이 손에 와야 한다.

`prefers-reduced-motion`이면 전부 끈다.

⚠️ `renderBars()`가 `#battle-player`를 통째로 다시 그린다. 효과는 **그 뒤에** 걸어야
한다 — 앞에 걸면 파편과 대미지 숫자가 같이 지워진다(실제로 그랬다).

### 🏘️ 마을 — 걸어다니는 로비

로비가 "활동 목록"이라 갈 곳을 **누르는** 것이지 **가는** 게 아니었다. 마을에서는 걸어서
문 앞에 선다. 미니게임은 그대로 두고 **입구만 세계로** 바꾼 것이라 위험이 적고 효과가 크다.

**플랫폼은 안 바꾼다.** 캔버스 2D로 충분하다 — 던전·러너가 이미 그렇게 돌아간다.
네이티브로 옮기면 전부 다시 짜면서 즉시 배포만 잃는다.

**마을을 크게 만들면 안 된다.** 걸어다니는 건 학습이 아니다. 오픈월드를 주면 아이가
10분 돌아다니고 2분 공부한다. 구역 높이는 118px로 조여 뒀다.

- **마을은 낮이다.** 어두운 밤 배경은 "멋있다"에는 맞지만 **"예쁘다"에는 안 맞는다.**
  잔디·돌길·붉은 지붕으로 밝게 간다. 밤은 던전이 맡는다
- **빈 땅을 남기지 않는다.** 나무·덤불·꽃·돌·울타리를 100개 가까이 뿌린다(`scatter`).
  건물과 길을 피해 자동으로 놓이고, 깊이 정렬에 같이 태워 캐릭터가 나무 뒤로 지나간다.
  휑한 땅이 "덜 만든 것처럼" 보이게 하는 가장 큰 원인이었다
- **걷기 두 프레임** (`Avatar.walkFrames`) — 다리를 모았다 벌렸다. 정지 이미지가 통통
  튀기만 하면 걷는 것으로 안 보인다
- 조이스틱은 **늘 왼쪽 아래에 보인다.** 터치해야 나타나면 처음 온 아이는 조작법을 모른다
- **길은 북쪽으로 난다.** 아래가 광장, 위로 갈수록 높은 등급 — 세계의 생김새가 곧 진도다.
  탑은 길 한가운데에 세운다 — 길을 따라 올라가면 문 앞에 선다
- **등급 하나 = 탑 하나.** 권(1~6)을 마을에 늘어놓으면 등급이 늘 때마다 6채씩 불어나
  11등급이면 66채가 된다. 탑은 하나로 세우고 **권은 탑 안에서 고른다** —
  권이 층층이 쌓인 모습이 그대로 "이 등급에 6권이 있다"는 설명이 된다
- 구역은 `LEVELS`/`TOWERS`에서 만든다. 등급을 추가하면 마을이 저절로 늘어난다
- **문 앞에 서면 노란 「들어가기」 버튼**이 뜬다. 자동 입장은 뺐다 —
  지나가다 실수로 들어가면 아이가 짜증낸다
- 탑 창문이 진행도다(층을 깰수록 불이 켜지고, 정복하면 지붕이 금색)
- 잠긴 탑은 회색에 자물쇠 문. 눌러도 지금의 문 앞 화면이 그대로 뜬다
- 📋 목록 버튼으로 기존 로비를 그대로 열 수 있다 — 마을에 문제가 생겨도 갈 곳이 있고,
  조이스틱이 어려운 경우의 대비이기도 하다
- `Game.home`이 'town'이면 미니게임이 끝난 뒤 마을로 돌아온다

### 🧱 층 구성과 난이도 기준

**한 층 = 단원의 1/4 (단어 6개).** 2등분이던 시절엔 한 층이 12개를 맡았는데,
한 층의 실제 접촉은 13~15회라 **단어당 1.1회**밖에 안 됐다. ★★★(카드)에는 힌트 없이
3번 정답이 필요하니 카드 하나에 같은 층을 세 번 돌아야 했다 — 반복이 아니라 노동이다.
6개로 줄이면 **단어당 2회 이상**이 되고, 층도 2~3분으로 짧아져 아이 집중 시간에 맞는다.

**난이도는 층 번호가 아니라 진행률로 정한다** (`normFloor`, `BAL.monster.refFloors = 10`).
층수로 재면 단원 8개짜리 탑(36층)이 티어가 더 높은 4단원 탑(18층)보다 세지는
**티어 역전**이 생긴다. 난이도는 티어가 정하기로 했으므로, 모든 탑의 층을 기준 층수로
환산해 곡선을 같게 만든다. 보상(경험치·골드)과 러너 속도도 같은 잣대를 쓴다 —
안 그러면 긴 탑이 층당 보상까지 더 받는다.

- 왕은 이미 환산된 층수(`BAL.king.floor`)를 쓰므로 정규화를 건너뛴다(`raw` 인자)
- 층수가 거의 두 배가 됐으므로 **층당 보상을 절반으로** 내렸다. 내용(단어)은 그대로인데
  총량만 두 배가 되면 안 된다
- 옛 세이브는 `MIGRATIONS[2]`가 층 번호를 옮긴다 (예: 늑대 탑 6/10층 → 11/18층).
  안 하면 다 깬 탑이 절반만 깬 걸로 보인다

### 👑 등급 왕과 입장 조건

**등급 왕** = 그 등급 **전체 단어**로 싸우는 별도 도전. 타워 층에 묻지 않고 로비의
「👑 왕의 방」에 따로 뒀다. 그래야 6권을 아직 안 산 아이도 카드만 모으면 도전할 수 있고,
언제든 재도전할 수 있다.

| | 값 | 왜 |
|---|---|---|
| 도전 조건 | 그 등급 전체 카드 **60%** (`BAL.cards.kingGate`) | 등급을 대표하는 시험 |
| 출제 범위 | 그 등급 **6권 전부** | 한 권만 파고든 아이는 못 깬다 |
| HP | 같은 층 보스 **×1.4** (문제 18~20개) | 더 늘리면 지루하다 |
| 공격력 | **×1.15** → 실수 **3번**이면 쓰러짐 | 길이가 아니라 **정확도**를 요구 |
| 보상 | 골드 ×3 · 칭호 · **다음 등급 해금** · **권장 레벨까지 상승** | 아래 참고 |
| 재도전 | 지면 **3분 대기** (`BAL.king.retryCooldownSec`) | 아래 참고 |

**진 뒤 3분 대기** — 왕은 연타로 뚫는 상대가 아니어야 한다. 실수 3번이면 끝나는 판을
바로 다시 열 수 있으면, 아이는 단어를 보는 대신 **운을 다시 굴린다**. 그러면 왕이 가벼워진다.

다만 3분을 그냥 기다리게 두면 벌이 될 뿐이라, 대기를 **복습 시간으로 바꿨다**:

- 패배 화면에 **그 판에서 틀린 단어**를 최대 8개까지 보여준다 (배틀이 오답 단어를 모아 넘긴다)
- 패배·대기 화면의 주 버튼은 "다시 도전"이 아니라 **📖 도감으로 가기**
- 남은 시간은 **벽시계**(`Date.now()`)로 잰다 — 게임을 꺼도 흐른다.
  켜 둔 채 버티게 만들면 기다림이 순수한 지루함이 된다
- 로비 왕 카드와 대기 화면의 남은 시간은 1초마다 갱신되고, 0이 되면 스스로 「도전!」로 바뀐다
- 대기는 등급별로 따로 걸린다 — 늑대 왕에게 졌다고 곰 왕까지 막히지 않는다

**왕을 이기면 열린 등급의 권장 하한까지 레벨을 올려 준다** (`raiseToLv`). 실력으로 문을 열어
놓고 들어가서 몹이 안 죽으면 연 보람이 없다. 왕은 그 등급 단어를 전부 알아야 깨므로,
레벨이 낮다는 건 "실력이 없다"가 아니라 "탑을 덜 돌았다"는 뜻일 뿐이다.

**해금 연출** (`js/fx.js`) — 왕을 **처음** 꺾어 새 등급이 열리는 순간에만 전체 화면으로 간다.
카드 60%를 모으고 그 등급 단어를 전부 상대해야 오는 순간이라, 모달 한 장으로 끝내면 무게가 안 산다.
(이미 깬 왕을 다시 이기거나, 다음 등급이 없으면 기존 모달로 간다)

왕이 부서짐 → 빛기둥과 방사광 → 왕관 쓴 아이가 새 등급의 탑들과 함께 떠오름 → 칭찬 → 보상.
**칭찬은 그 판의 실제 오답 수로 고른다** — "잘했어"는 아이도 빈말인 걸 안다:

| 틀린 단어 | 문구 |
|---|---|
| 0개 | **PERFECT** · "한 번도 틀리지 않았어. 왕은 손 쓸 틈조차 없었다." |
| 1~2개 | **SPLENDID** · "흔들리고 나서 이기는 게 더 어려운 거야." |
| 3개+ | **BRAVE** · "포기 안 하는 게 제일 어려운 거야." |

밑에 근거 한 줄을 붙인다 — "늑대 등급 단어 **575개**를 전부 상대했다".
연출은 4초쯤이고, 화면 아무 데나 누르면 끝까지 감긴다(다 본 아이를 기다리게 하지 않는다).

**타워 입장 조건** — 둘 중 하나면 된다:

```
레벨 ≥ 권장 하한(lvRange[0])   또는   이전 등급의 왕을 잡았다
```

- 낮은 등급은 권장 하한이 낮아 **항상 열려 있다** — 고레벨이 자기 티어를 복습하러 올 수 있다
- 한 등급 안의 6권은 서로 막지 않는다 — 학원이 3권을 내주면 1·2권 없이 바로 할 수 있어야 한다
- 앞 등급에 타워가 없으면 그 왕은 **존재하지 않으므로**, 타워가 있는 가장 가까운 앞 등급의
  왕을 가리킨다(`prevKingLevel`). 그것도 없으면 왕 경로를 숨긴다 — 없는 길을 안내하면
  아이는 갈 데를 잃는다
- 잠긴 탑도 **문을 두드릴 수 있다**. 막는 화면이 아니라 "여기가 어떤 곳인지" 보여주는
  화면이라, 등급·단어 수·문지기 대사를 띄우고 **지금 갈 수 있는 가장 센 탑**으로 보내 준다
- 문 앞에서 **💪 그래도 들어간다**를 고를 수 있다(`forceOpen` → `player.forced[towerId]`).
  학원이 앞선 책을 내주는 일은 실제로 있다 — 레벨 때문에 숙제를 못 하면 이 게임은 쓸모가 없다.
  대신 무슨 일이 벌어지는지는 정확히 말하고(몹은 하나도 안 약해진다), 로비 카드에 **💪 강행**을 단다.
  한 번 고르면 그 탑은 계속 열려 있다 — 들어갈 때마다 같은 경고를 다시 보게 하면 그건 잠금이나 마찬가지다
- 모든 탑을 한 번에 열려면 설정에 **🔓 타워 잠금 끄기**(`settings.noLock`)

등급 정의는 `js/state.js`의 `LEVELS` 하나에 모았다. 새 등급을 만들면 여기에 한 줄 추가하고,
타워 데이터에 `level`/`book`을 적으면 왕·입장조건이 자동으로 따라온다.
왕 그림은 `Art.king(animal)` — 전용 그림이 없으면 그 등급 동물에 왕관과 오라를 얹는다.

### 정복 보너스에는 상한이 있다

타워를 정복하면 영구 능력치가 붙는데(`clearBonus`), 등급 11개 × 6권 = **66개**를 다 깨면
합계가 **+500%**가 되어 가장 어려운 MS-B가 2방에 죽는 역전이 생긴다.
그래서 `BAL.player.clearBonusCap`(현재 **+100%**)으로 종류별 총합을 막았다.

| 정복 보너스 | MS-B(tier 3.0) 5층 몬스터 |
|---|---|
| 0% | 8방 |
| **+100% (상한)** | **6방** |
| +250% | 3방 |
| +500% (상한 없음) | 2방 |

상한에 닿은 뒤에도 **칭호는 계속 모인다** — 수집물은 그대로 두고 숫자만 멈춘다.

**정복 칭호도 세계에 맞춘다.** 곰 탑 1 → `숲의 파수꾼`, 곰 탑 2 → `숲의 수호자`.

- **타워 1개 = 단어 파일 1개** (`data/*.js`). 게임 엔진은 공통.
- 타워별 진행도는 분리, **캐릭터(레벨·골드·장비·펫)는 공용**.
- **층 생성 규칙:** 단원 1개 → 일반 층 2개(단어 절반씩). 단원 2개마다 **보스 층**.
  마지막 단원 뒤엔 무조건 보스. (단어 6개 이하 단원은 두 층 모두 전체 단어)
  → 6단원이면 12 + 3 = 15층.
- 틀린 단어는 다른 층에서 다시 등장(복습, 간격 반복).

## 4. 한 층의 흐름 (완급 조절)

```
🔍 정찰(예습)  →  [탐험 파트]  →  [마무리 파트]  →  다음 층!
                  저강도·판단      고강도·반사
```

**층마다 조합이 달라진다.** 탐험 파트와 마무리 파트를 각각 풀에서 하나씩 뽑는다
(`BAL.stages`). 45층을 같은 순서로 가면 아무리 좋은 게임도 지겹다 —
**미니게임 개수가 아니라 조합이 다양성을 만든다.** 층 길이는 2~3분으로 유지된다.

| 파트 | 지금 있는 것 |
|---|---|
| 탐험 (저강도) | 🧭 미로 · 🔐 금고 방 |
| 마무리 (고강도) | 🪜 계단 러너 |

배틀은 독립 스테이지가 아니라 **탐험 파트 안에서 튀어나온다** —
미로에선 길목의 몬스터로, 금고 방에선 금고지기로. 그래야 한 층이 길어지지 않는다.
같은 탐험 파트가 두 층 연달아 나오지는 않는다(`avoidRepeat`). 보스 층은 배틀만.

**미니게임을 추가하려면**: 화면 파일 하나 + `Game.STAGES`에 등록 + `BAL.stages` 목록에 id.
스테이지는 `(run, done) => ...` 하나만 지키면 된다 — 끝나면 `done()`을 부른다.
화면이 다음 화면을 직접 부르면 안 된다(그러면 조합을 못 바꾼다).

**🔍 정찰** — 처음 보는 단어가 배틀에서 갑자기 나오면 학습이 아니라 찍기가 된다.
층 시작 전에 카드를 한 장씩 보여주고 마지막에 전체 요약. "다음"은 잠깐 뒤에 켜진다(마구 넘기기 방지).
이미 카드가 있는 단어 · 보스 층 · 이미 깬 층 재도전은 생략. 설정에서 끌 수 있다.

**⚡ 이건 알아! (지름길)** — 정찰 중 아는 단어는 그 자리에서 스펠링을 맞혀 ★★★ + 카드를 즉시 딸 수 있다.
단, **정규 각인 시험보다 어렵게** 만들었다: 도움 글자 없음 · 함정 글자가 더 많음 · 층당 1회.
그래야 지름길이 정규 경로를 대체하지 않는다. 실패해도 벌은 없다(★ 유지).

| 구간 | 학습 액션 | 조작 |
|---|---|---|
| **미로** | 문에 한글 뜻이 적혀 있고 바닥에 영어 열쇠가 흩어져 있음. 열쇠는 주운 만큼 꾸러미에 모이고, 문 앞에서 **어떤 열쇠로 열지 고른다**(= 단어 판단 순간). 틀린 열쇠는 "삐-" 부서지고 HP 소폭 감소. 안개(fog)로 탐험감. 보물상자(골드). 캐릭터·펫은 격자와 분리된 스프라이트 레이어로 부드럽게 이동 | 방향키 / 스와이프 / D-pad |
| **배틀** | 몬스터 머리 위 문제(뜻↔단어, 듣기). 4지선다에서 맞는 걸 터치하면 공격. **빨리 답하면 크리티컬.** 틀리면 몬스터가 공격 | 터치 |
| **러너** | 3레인 자동 달리기. 미션(뜻)에 맞는 영어 단어 타일에 부딪히면 수집, 틀린 타일은 피해야 함. 코인 수집 | 위/아래 |
| **보스 층** | HP 큰 보스와 긴 배틀. 그동안 나온 단어 전부 섞어서 출제. 클리어 시 펫 드랍 | 터치 |

**문은 막다른 길에만 놓는다.** 통행로에 두면 열쇠를 주우러 되돌아가야 해서 답답하다.
막다른 길이 하나도 없는 미로면 문이 길을 막지 않게 한다.

### 미니게임이 자리를 얻는 조건

아무 미니게임이나 넣으면 안 된다. 둘 다 만족해야 한다.

1. **액션 자체가 단어 판단일 것** (1장 목표). 퀴즈 풀고 캐릭터가 점프하면 껍데기다.
2. **기존과 다른 능력을 시험할 것.** 아니면 옷만 갈아입은 같은 게임이다.

| | 시험하는 능력 |
|---|---|
| 미로 문 | 뜻 → 영어. 시간 압박 없이 신중하게 하나 고르기 |
| 금고 방 | 여럿을 **동시에 놓고 배치**. 확실한 것부터, 애매한 건 소거법으로 |
| 배틀 | 양방향 4지선다 + **시간 압박** |
| 러너 | 뜻 → 영어 + **반사신경·공간 판단** |
| 각인/필살기 | **철자 쓰기** |

아직 안 하는 것: 품사 분류(`pos` 100% 보유) · 과거형(`past` 32%) ·
문맥 빈칸(`ex` 0% — 예문을 직접 써야 해서 비용이 크다). [BACKLOG](BACKLOG.md) 참고.

### 🔐 금고 방

자물쇠마다 한글 뜻이 적혀 있고, 아래에 단어 카드가 흩어져 있다(오답 카드가 섞여 있다).
카드를 고르고 자물쇠에 꽂는다.

- 맞으면 열리고 카드는 소모. **틀리면 그 카드가 부서져 영영 못 쓴다** + HP 소폭 감소
- 여분 카드만큼은 틀려도 되지만, 그 이상 틀리면 못 여는 자물쇠가 생긴다
- 중간쯤 열면 **금고지기**가 깨어나 배틀 1회
- 못 열어도 진행은 막지 않는다. 답을 보여주고 넘어간다 (관문이 아니라 층 안의 한 구간)

**왜 재밌나:** 아는 것부터 꽂으면 남은 게 줄어 마지막이 쉬워진다.
반대로 자신 있던 걸 틀리면 뒤가 무너진다. **꽂는 순서 자체가 게임**이 된다.

## 5. 성장 시스템

### EXP vs 골드 — 역할이 다름
| | EXP | 골드 |
|---|---|---|
| 용도 | 레벨업(자동) | 상점 소비 |
| 획득 | 정답마다 | 층 클리어·보스·보물상자·코인 |
| 의미 | "얼마나 강해졌나" | "뭐 살까" |

### 밸런스 원리 (2026-08-31 개편)

> 수치는 전부 [`js/balance.js`](js/balance.js)에 있다. 여기 있는 건 **왜 이 모양인지**다.
> 아래 검증표는 [`tools/test.js`](tools/test.js)가 매번 확인한다.

문제였던 것: 공격력은 **레벨 + 무기(더하기)**로 오르는데 몬스터 HP는 **그 타워의 층수**로만 정해져서,
바벨에서 키운 캐릭으로 새 타워 1층에 가면 한 방에 죽었다. 타워를 추가할수록 심해지는 구조.

**① 무기는 더하기가 아니라 곱하기(%)** — 레벨 성장을 골드로 건너뛰지 못하게.

**② 타워마다 "설계 기준 레벨 구간"(`lvRange`)과 "티어"(`tier`)**

```
기본공격력 = atkBase + Lv×atkPerLv           (무기 제외)
내 공격력  = 기본공격력 × (1 + 무기% + 정복보너스%)
기준레벨   = lvRange[0] + (clamp(내Lv, lvRange) − lvRange[0]) × growth
몬스터 HP  = 기본공격력(기준레벨) × (base + 층×perFloor) × 티어
몬스터 공격력 = 기본HP(내Lv) × (base + 층×perFloor) × 티어 (보스는 ×bossAtkMul)
미로 오답 / 러너 충돌 = 기본HP(내Lv) × 비율 × 티어
```

- **clamp**: 구간을 넘어선 레벨은 그대로 초과 화력 → Lv50이 바벨에 가면 한 방 (통쾌함 유지)
- **growth (0.7)**: 몬스터는 내 성장의 70%만 따라옴 → 같은 타워 안에서도 레벨업이 체감되게
  ※ 실제로는 체감이 예상보다 작다. [BACKLOG](BACKLOG.md) 「밸런스에서 살펴볼 것」 참고
- **티어**: 타워 데이터에 숫자 하나만 넣으면 난이도가 맞춰짐.
  **티어는 "반(班)"에 매긴다** — 같은 동물이면 🔥 개수가 같다. 권수는 난이도가 아니라
  단어 양의 차이라 같은 동물 안에서는 +0.05씩만 올린다 (곰 1.5 / 1.55 / 1.6 < 독수리 1.7).
  등급별 tier·lvRange 표는 `data/jeongsang.js` 주석에 있다
  - 화면에는 **티어 숫자를 절대 보여주지 않는다** — 아이가 읽을 수 없다.
    `tierFire()`로 🔥 개수로 바꿔서 보여준다 (1.0 → 🔥 · 1.5 → 🔥🔥 · 2.0 → 🔥🔥🔥).
    ⭐은 단어 마스터리(★0~3)에 이미 쓰고 있어서 🔥을 골랐다
- **적 피해는 최대HP가 아니라 기본HP 기준**. 최대HP를 올려주는 보상이 스스로 상쇄되면 안 되니까
- 로비 타워 카드에 **권장 레벨**과 ⚠️/👍/😎 표시

검증 (Lv8 + 불꽃검, 공격력 60):

| | 개편 전 | 개편 후 |
|---|---|---|
| 바벨 5층 몬스터 | **1방** | 3방 |
| 곰 탑 1 5층 몬스터 | 1방 | 4방 |
| 곰 탑 1 5층 보스 | 4방 | 11방 |
| Lv50이 바벨 5층 | 1방 | 1방 (의도대로) |

**③ 성장 곡선은 "다 합쳤을 때"로 검증한다** (2026-09-01)

개별 수치를 따로 정하면 합쳤을 때 게임이 망가진다. 실제로 셋이 동시에 어긋나 있었다.

| | 기준 | 어긋났을 때 생긴 일 |
|---|---|---|
| 경험치 곡선 | 콘텐츠를 한 번씩 깨면 **최고 권장 레벨 근처**까지 | 55층을 다 깨도 Lv15인데 타워는 Lv48을 상정 → 벽 |
| 필살기 배율 | 일반 몬스터는 한 방, **보스는 2~3방** | 카드를 다 모으면 20.6배라 최종 보스도 한 방 → 배틀이 사라짐 |
| 몬스터 공격력 | **5~10번** 틀리면 쓰러짐 | 바벨 1층에서 17번을 틀려야 죽어 긴장감이 없음 |

세 기준 모두 [`tools/test.js`](tools/test.js)의 「성장 곡선」 섹션이 매번 확인한다.
수치를 손대면 어디가 깨졌는지 바로 나온다.

**④ 골드 경제** — 한 층 약 130G. 은검 6층, 강철검 16층, 불꽃검 39층, 용의검 94층.
**이미 깬 층 재도전은 골드만 30%**로 줄인다. 경험치는 그대로 — 다시 푸는 건 공부니까.

### 레벨 = 진짜 강함 (골드로 못 삼)
- 데미지 숫자가 화면에 **팍!** 뜸 → 레벨업 후 커진 게 바로 보임
- 캐릭터 외형 변화: Lv1 🧒 → Lv5 🧑 → Lv10 🦸 → Lv20 🧙

### 레벨업 스킬 (게임 방식이 바뀜)
| Lv | 스킬 | 효과 |
|---|---|---|
| 3 | 🔥 더블 어택 | 2연속 정답마다 추가 공격 |
| 5 | 👁️ 투시 | 미로에서 열쇠 위치가 안개 너머로 보임 |
| 8 | 🛡️ 실드 | 하루 1회, 틀려도 데미지 0 |
| 12 | ⚡ 대시 | 러너에서 첫 충돌 무시 |
| 20 | 💫 필살 강화 | 필살기 위력 1.5배 |

### 필살기 — 카드가 전투 위력이 된다
연속 정답 3번 → 게이지 참 → 스펠링 특별 문제 → 맞히면 대미지 폭발. **틀려도 피해는 없다**(게이지만 소모).
위력 = 공격력 × (2 + 카드포인트/25) → 카드 0장이면 2배, 전부 모으면 8.9배.
공부한 만큼 전투가 세지는 유일한 통로라, 카드 수집에 전투적 이유가 생긴다.

### 레벨 전용 구역 (잠긴 문 = 목표)
- Lv5 🏟️ **투기장**: 익힌 단어로 무한 배틀. 몇 마리 잡나 기록.
  레벨 오를수록 기록이 눈에 띄게 오름 → "강해진 실감"
  - **오래 버틸수록 조여든다**: 제한시간이 8초에서 5초까지 줄고, 몬스터는 계속 세진다
  - **🏳️ 여기까지 버튼**: 죽을 때까지 기다리지 않고 나온다. 골드·경험치는 잡을 때마다
    이미 받았고 기록도 남는다. *나갈 길이 있어야 어렵게 만들 수 있다* —
    나가려면 죽어야 하는데 안 죽으면, 재미가 아니라 갇힌 것이다
- Lv10 🕳️ **지하 던전** (+ 카드 25장): 무너지는 다리 위의 추격 — 아래 참고
- Lv20 ⛰️ **하늘섬** (+ 카드 60장): 구름을 밟고 정상까지 — 아래 참고

### 🕳️ 지하 던전 — 무너지는 다리

**끝없는 던전은 만들지 않았다.** "몇 층까지 갔나"는 순위표를 보는 어른의 동기다.
아이에게는 이겼다/도망쳤다가 필요하다. **한 판 8칸, 2~3분, 끝이 분명하다.**

규칙이 그림 하나로 설명된다 — 설명이 필요 없는 게임이 좋은 게임이다:

> 뜻에 맞는 발판만 밟는다 → 틀리면 발판이 부서지고 몬스터가 한 칸 다가온다 → **3번이면 잡힌다**

제한시간이 숫자가 아니라 **쫓아오는 놈**이라는 게 핵심이다. 판단이 곧 발걸음이고,
틀리면 문제에 X표가 뜨는 게 아니라 내가 떨어진다.

**고른 단어가 실제로 다리가 된다.** 맞히면 그 단어가 새겨진 널빤지가 위에서 날아와
낭떠러지에 쿵 박히고(흔들림+먼지), 아이가 그 위를 달려 건넌다. 맞혔다는 사실과
앞으로 나아간다는 사실이 같은 동작이어야 한다 — 카메라만 흘러가면 아무 느낌이 없다.

**시간은 낭떠러지까지의 거리다.** 게이지가 아니다. 그리고 갈수록 조인다:

| | 값 |
|---|---|
| 첫 칸의 여유 | `timeLimit` 6초 |
| 한 칸마다 | `timeStep` 0.32초씩 줄어듦 |
| 하한 | `timeMin` 3.4초 (8칸째 ≈ 3.8초) |
| 실패 후 | `failRunway` — 기어올라온 자리가 곧 낭떠러지 앞이라 **남은 시간이 절반** |

실패가 다음 판단을 더 촉박하게 만든다. 벌이 아니라 압박이다.

| | |
|---|---|
| 단어 | **★이 낮은 것부터** — 던전이 곧 오답노트다. 지금 게임에 없던 축 |
| 한 판의 단어 수 | `wordsPerRun` **4개**. 8칸이니 하나가 **두 번씩** 나온다 |
| 만난 적 없는 단어 | 안 나온다. 처음 보는 단어로 쫓기면 학습이 아니라 운이다 |
| 져도 | 카드·★·경험치 그대로. 공부 결과를 벌로 뺏으면 다시 안 온다 |
| 보물 상자 | **밀린 각인 시험**만 — 카드 총량이 안 바뀌어 왕 조건·오라를 안 건드린다 |
| 영구 강화 | 없음. `clearBonusCap`으로 겨우 잡아둔 성장 곡선에 축을 더하지 않는다 |
| 골드 | 한 판이 **보스 한 층보다 적게** (회귀로 고정) — 복습이지 파밍이 아니다 |

**서로 다른 단어 8개를 한 판에 물으면 외우기가 아니라 시험이다.** 4개를 뽑아 두 번씩
내보낸다(연달아 같은 건 안 나오게). 틀린 단어는 그 판이 끝나기 전에 한 번 더 온다 —
놓친 채로 끝내지 않는다. 오답 보기는 넓은 후보(`words` 40개)에서 뽑는다: 4개만 쓰면
보기가 매번 똑같아져 아이가 위치를 외운다.

달리기 전에 그 4개를 **한 번 보여준다**(`brief`, `settings.preview` 를 따른다).
모르는 단어로 쫓기면 학습이 아니라 공포다 — 타워의 정찰과 같은 이유다.
잡히거나 도망친 뒤에도 같은 목록을 다시 보여준다.

**프롤로그** (`Dungeon.prologue`): 추락 → 어둠 → 횃불 → 발견 → 도망. 약 6초.
횃불로 비추다 만난 그것이 곧 쫓아오는 놈이라, 연출과 규칙이 하나로 이어진다.
지연은 CSS가 아니라 JS의 표(`FULL`/`SHORT`)에서 준다 — **두 번째부터는 발견 컷부터 2초**.
다 본 아이를 매번 기다리게 하지 않는다. 탭하면 언제든 끝까지 감긴다.

수치는 전부 `BAL.dungeon`.

### ⛰️ 하늘섬 — 연쇄가 곧 높이다

**왜 또 만드나.** 지금 게임엔 *잘한 걸 즉시 갚아주는 가속*이 없었다.
던전은 8칸을 다 맞혀도 8칸이고, 배틀은 빨리 맞혀도 한 대다.
하늘섬에서는 **3연속이면 바람을 타고 두 칸씩** 오른다 — 잘하는 게 눈에 보인다.

규칙이 네 줄로 끝난다:

> 맞는 구름을 밟는다 → 고도 +1 (바람이면 +2)
> 틀리면 고도 −1. 발밑 구름이 다 옅어져도 −1
> 6칸마다 🏝️ 섬 — 그 아래로는 안 떨어진다. 섬에서 또 틀리면 섬이 무너져 **추락**
> **24칸이 정상.** 도달하면 끝난다

**시간은 게이지가 아니라 발밑 구름이다.** 서 있을수록 옅어지고 작아진다. 던전은
낭떠러지가 옆에서 다가오고, 하늘섬은 발밑이 사라진다 — 같은 원리, 다른 감각.
그리고 하늘섬의 시계는 던전보다 훨씬 느슨하다(9→5.5초 vs 6→3.4초).
**여기서 조이는 건 시간이 아니라 되돌릴 수 없는 고도**여야 하기 때문이다.

| | |
|---|---|
| 끝 | **정상 24칸.** 무한 기록은 투기장이 이미 갖고 있다. 아이에겐 "이겼다"가 필요하다 |
| 안전지대 | 6칸마다. `floorAlt = floor(alt/6)*6` — 바람으로 건너뛰어도 안 놓친다 |
| 첫 구간 | 땅(고도 0)은 안 무너진다. **규칙을 배우다 죽지 않는다** |
| 단어 | **★이 높은 것부터** — 던전(오답노트)과 정반대. 아는 걸 자랑하는 무대 |
| 한 판의 단어 수 | `wordsPerRun` 8개. 24칸이니 하나가 **세 번씩** |
| 골드 | **새로 오른 칸에만** 준다. 안 그러면 오르내리며 파밍이 된다 |
| 져도 | 올라간 만큼의 골드는 이미 받았다. 카드·★·경험치도 그대로 |
| 한 판 보상 | 보스 한 층보다 적게 (회귀로 고정) |

**인트로** (`SkyIsland.intro`): 올려다봄 → 구름이 발 앞에 내려앉음 → 올라가자. 약 4초.
던전과 같은 방식으로 지연을 JS의 표(`FULL`/`SHORT`)에서 주고, **두 번째부터는 짧게**.

마을에서는 **북쪽 길 끝**에 관문이 있다. 마을에서 제일 먼 곳이 제일 나중에 열리는
곳이라야 목표로 보인다. 수치는 전부 `BAL.sky`.

### 상점
- 소모품: 💡힌트(첫 글자) · 🧽지우개(오답 2개 제거) · 🧪물약(최대 HP 40% 회복)
- 무기 6단계 (곱하기 %), 모자 3종, 코스튬 5종
- 펫: **보스 드랍 전용 — 골드로 못 삼** 🐱 → 🦉 → 🐲
- 가격·수치는 `js/state.js`의 `WEAPONS`/`ITEMS`/`HATS`, `js/avatar.js`의 `OUTFITS`

### 단어 마스터리 (공부 안 하고 넘어가는 걸 막는 장치)
- 단어마다 ★0~3. 힌트 없이 맞추면 +1, 틀리면 −1.
- **힌트/지우개를 쓴 문제는 ★이 안 오름.** → 스스로 힌트 안 쓰려 함.
- ★가 낮은 단어가 더 자주 나옴.

## 6. 단어 카드

**설계 의도:** 4지선다 ★★★는 소거법으로도 되는 "알아보는 능력"이다.
카드는 **직접 써내는 능력**으로만 얻게 해서, 공부해야만 열리는 성장 축을 만든다.

```
★★★ 달성 → 각인 시험 대기 → 스펠링 시험 통과 → 카드 획득
```
- **각인 시험**: 키보드가 아니라 **글자 타일 조합**(모바일 친화).
  층 클리어 후 최대 2장, 나머지는 도감에서 언제든
- **실패해도 벌 없음**: ★★★와 대기 상태 유지, 재도전 가능
- 힌트/지우개를 쓴 문제는 ★가 안 오르므로 카드도 안 나옴 (기존 규칙 유지)
- 기존 저장의 ★★★ 단어는 접속 시 대기열로 소급(`backfillCards`)

### 등급 (영어 글자 수 + 구 여부로 자동 판정 — 새 타워도 그대로 작동)
| 등급 | 조건 | pt | 미리 주는 글자 | 예 |
|---|---|---|---|---|
| 일반 | ≤4 | 1 | 0 | buy |
| 희귀 | 5~7 | 2 | 1 | palace |
| 영웅 | 8~10 또는 구 | 3 | 3 | raindrop, in fact |
| 전설 | 11+ | 5 | 40% | double-decker |

현재 분포: 바벨 48장(70pt) · 곰 탑 1 48장(102pt) · 곰 탑 2 96장(206pt) = **192장 378pt**

### 보스 관문 (부드러운 관문)
- **일반 층은 자유**, **보스 층만** 해당 단원 카드 60%(`BAL.cards.bossGate`) 필요
- 부족하면 모자란 단어를 보여주고 **📖 복습 배틀** 제공 —
  허수아비(ATK 0)라 맞아도 안 아프고, 카드 없는 단어만 출제

## 7. 수집 보상 — 오라 · 칭호

옷과 다른 슬롯이라 상점 코스튬과 안 겹치고, 캐릭터 주변에서 움직여
미로·배틀·러너 어디서든 보인다. **골드로는 살 수 없다.**

### 오라는 타워에 매여 있지 않다 (2026-09-01 재설계)

처음엔 **단원 1개 = 오라 1개**였다. 두 가지가 무너졌다:

1. **남발** — 단원 18개면 오라 18종. 매번 나오니 하나하나가 안 특별하다.
2. **확장 불가** — 타워를 추가할 때마다 오라를 그만큼 새로 그려야 하고,
   재사용하면 "🎉 새 오라!"라며 이미 가진 걸 주게 된다.
   (밸런스 개편 때 고쳤던 "타워를 추가할수록 심해지는 구조"와 같은 병이다)

그래서 **전체 진행도에 거는 마일스톤**으로 바꿨다. 조건은 `js/avatar.js`의 `AURAS[].need`:

| 조건 | 뜻 |
|---|---|
| `cards: N` | 지금까지 모은 카드 총 N장 — **어느 타워에서 모았든 상관없다** |
| `tier: T` | 티어 T 이상인 타워를 **꼭대기까지 깼을 것** — 상위 오라는 어려운 타워를 거쳐야 나온다 |

화면에는 티어 숫자 대신 `난이도 🔥🔥 이상 타워 정복`으로 나오고,
어느 타워가 해당하는지도 이름으로 알려준다(`Cards.tierList`).
타워 카드에도 같은 🔥 표시가 있어야 조건을 읽을 수 있다.

| 오라 | 카드 | 티어 |
|---|---|---|
| ✨ 반짝이 · 🌊 물결 · 💫 유성 · 🦋 요정날개 | 15 · 35 · 60 · 90 | — |
| 🔥 불꽃 · ⚡ 번개 | 120 · 150 | 1.5+ |
| 🪽 천사날개 · 🌈 무지개 · 🌙 달빛 · 🐉 용 | 180 · 210 · 250 · 300 | 2.0+ |

- **지금 콘텐츠(192장)로 7종까지.** 나머지 3종은 다음 타워를 만들 이유가 된다
- 스테이지를 아무리 늘려도 이 목록을 안 건드려도 된다. 타워 데이터에 오라 필드가 없다
- 조건이 지금 콘텐츠로 닿는지는 `node tools/validate.js`가 매번 확인한다

### 단원 완성 = 골드
오라가 빠진 자리는 골드가 채운다 (`BAL.gold.unitComplete` × 티어).
성취감은 남기되, 진짜 희귀한 보상은 마일스톤으로 몰았다.

### 타워 완전 정복 = 칭호 + 영구 보너스

용어를 나눠 쓴다 — **정복** = 꼭대기 층까지 깸(오라 조건이 보는 것),
**완전 정복** = 그 타워의 카드를 전부 모음.

타워의 카드를 **전부** 모으면 `clearBonus` — 밸런스 조절 지점을 타워 수만큼으로 묶어 둔다.
바벨 공격력 +10% · 곰 탑 1 최대HP +15% · 곰 탑 2 공격력 +20%

도감 위쪽 띠에서 오라 10종의 조건과 진행도를 항상 볼 수 있다 —
목표가 눈에 보여야 모으고 싶어진다.

## 8. 듣기 / 말하기

- **말하기: 만들지 않음.**
- 브라우저 내장 TTS 사용(외부 API 없음). 한국어 기기에서 한국어 목소리가 영어를 읽어버리지
  않도록 `englishVoice()`로 영어 목소리를 직접 골라 준다.

### 설정을 둘로 나눈 이유 (2026-09-01)

처음엔 "듣기" 설정 하나가 두 가지를 같이 켰다. 그래서 **발음을 들으려면 난이도까지 올려야 했다.**
정찰은 처음 만나는 단어를 보여주는 자리인데, 기본값이 OFF라 아이가 발음을 한 번도 못 들을 수 있었다.

| 설정 | 기본 | 하는 일 |
|---|---|---|
| 🗣️ **발음 읽어주기** (`say`) | **ON** | 정찰에서 카드가 뜨면 자동으로 읽어준다. **학습 도움 — 난이도와 무관** |
| 🎧 **듣기 문제** (`listen`) | OFF | 배틀에 "글자 없이 소리만 듣고 뜻 고르기"가 섞인다. **난이도 옵션** |

- 🔊 버튼은 **설정과 무관하게 항상 있다.** 자동 재생만 끌 수 있게 한다 —
  설정 하나로 기능이 통째로 사라지면 안 된다
- 들려줘도 답이 새지 않는 자리에만 둔다: 정찰 카드·요약, 도감의 가진 카드,
  배틀에서 영어가 이미 보이는 문제(w2m). 뜻→영어 문제(m2w)에는 없다

## 9. 단어 데이터 형식

`data/*.js` 파일 하나가 타워 하나. 자세한 건 [README.md](README.md).
넣은 뒤 `node tools/validate.js`로 확인한다.

| 필드 | 필수 | 예 |
|---|---|---|
| unit | ✅ | 1 |
| w (영어) | ✅ | apple |
| m (한글 뜻) | ✅ | 사과 |
| pos (품사) | | n |
| ex (예문) | | I ate an apple. (책 예문 복사 금지) |

타워 단위 필드: `id` `name` `desc` `emoji` `roof` `tier` `lvRange` `clearBonus`

## 10. 그래픽

단계마다 배포하고 아이 반응을 보며 진행한다. 지나온 단계는 [CHANGELOG](CHANGELOG.md).

- **캐릭터** — 레이어 조합 SVG(`js/avatar.js`): 머리 4종 × 머리색 6 × 피부 3.
  코스튬 추가 = `OUTFITS` 항목 + `outfitSvg()` 분기
- **몬스터** — 9종 + 보스 5종 + 펫 3종 SVG (`js/art.js`, viewBox `0 0 120 120`)
- **로비** — 타워 일러스트(깬 층만큼 창문에 불), 밤하늘 별 + 도시 실루엣
- **연출** — 타격 번쩍/움찔/베기, 크리티컬 화면 번쩍, 클리어 폭죽 + 팡파레

## 11. 코드 구조

빌드 도구 없음. `index.html`이 스크립트를 순서대로 로드하고, 각 파일은 전역 IIFE 하나를 만든다.
**로드 순서가 곧 의존성 순서다.**

```
data/*.js            타워 단어
      ↓
balance.js           난이도·보상 수치
state.js             저장 + 성장 수식
      ↓
words.js  cards.js   출제 · 층 구성 · 카드
      ↓
art  avatar  audio  ui        그리기 · 소리 · 공용 UI
      ↓
lobby  maze  battle  runner  preview     화면
      ↓
main.js              층 진행 오케스트레이터
```

**규칙: 화면은 다음 화면을 직접 부르지 않는다.** `done()`을 부르고,
어떤 스테이지가 이어질지는 `Game.nextStage()`가 정한다.
(예전엔 `maze.js`가 `Runner.start()`를 직접 불렀다 — 그래서 조합을 바꿀 수 없었다)

**규칙: 아래 레이어가 위 레이어를 부르지 않는다.**
(지금 `cards.js`·`preview.js`가 `Lobby.render()`를 직접 부르는 위반이 있다 → [BACKLOG](BACKLOG.md))

파일이 25개를 넘으면 ES module 전환을 검토한다. 지금 안 하는 이유는
`index.html` 더블클릭 실행이 막혀서 — README의 사용법이 깨진다.

### 무엇을 어디에 쓰나
| 바꾸고 싶은 것 | 열 파일 |
|---|---|
| 난이도·보상·속도 | `js/balance.js` |
| 상점 품목·가격·스킬·구역 | `js/state.js` 테이블 |
| 단어 | `data/*.js` |
| 화면 모양·색 | `css/style.css` |

## 12. 기술 단계

### 1단계 (현재) — 브라우저만, 서버 없음
- 순수 HTML/CSS/JS. 빌드 없음. GitHub Pages로 배포 → 링크 하나.
- **자동저장:** 단어 하나 맞출 때마다 localStorage에 저장.
- **저장 코드:** 진행 상황을 문자열로 내보내기/불러오기 → 다른 기기로 옮기기.
- **백업:** 불러오기·초기화·손상 직전 상태를 하나 보관, 로비에서 되돌릴 수 있음.
- **세이브 버전:** `SAVE_VERSION` + `MIGRATIONS`로 구조 변경 시 순차 변환.

### 2단계 — 게임이 재밌다고 검증된 후에만
클라우드 저장·랭킹·레이드·선생님 대시보드. 자세한 건 [BACKLOG](BACKLOG.md).
`loadState`/`saveState`만 바꾸면 되도록 저장 경로를 한 곳에 모아 뒀다.
